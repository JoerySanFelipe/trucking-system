# Client & Project Documentation Folder

Place any project documentation, client requirements, rate sheets, contracts, sample receipts, or design mockups in this directory (`c:/kudecode/porbido-trucking/docs/`).

### Recommended File Types:
- `.md` / `.txt` - System documentation & requirements
- `.pdf` / `.png` / `.jpg` - Sample POD delivery receipts, Cargill contracts, invoice templates
- `.json` / `.csv` / `.xlsx` - Sample fleet data, driver lists, or route pricing logs

---
Whenever you add new files here, just let me know (e.g. *"read the docs folder"* or *"scan the file in docs/"*), and I will thoroughly analyze them to integrate the context into our TMS codebase!
